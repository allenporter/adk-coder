#!/usr/bin/env bash
# script/test: Run tests

set -e

cd "$(dirname "$0")/.."

echo "==> Running tests with coverage..."

if command -v uv >/dev/null 2>&1; then
  uv run pytest --cov=adk_coder --cov-report=term-missing "$@"
else
  pytest --cov=adk_coder --cov-report=term-missing "$@"
fi
